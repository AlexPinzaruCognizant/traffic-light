<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrafficLight" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TrafficLight" parent="Base.Theme.TrafficLight" />

    <style name="ToolbarStyle" parent="@style/Widget.Material3.Toolbar">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Toolbar</item>
        <item name="titleTextColor">#FFFFFF</item>
        <item name="navigationIconTint">#FFFFFF</item>
    </style>

    <style name="ThemeOverlay.App.Toolbar" parent="">
        <!-- Tint the menu and overflow icons -->
        <item name="colorControlNormal">#FFFFFF</item>
    </style>
</resources>